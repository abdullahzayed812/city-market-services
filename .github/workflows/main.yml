name: CityMarket CI/CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to production
    runs-on: self-hosted
    steps:
      - name: Run deploy.sh --update
        run: |
          cd ${{ vars.PROJECT_DIR }}
          ./scripts/deploy.sh --update
